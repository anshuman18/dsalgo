# Collections of DS and Algo Problems

* To run the unit tests : mvn test
* To run the unit tests with jacoco code coverage and fail when the coverage falls below the coverage threshold  : mvn clean verify